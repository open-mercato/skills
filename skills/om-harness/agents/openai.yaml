interface:
  display_name: "Agent Harness"
  short_description: "Shared staged-only packet and review runtime"
  default_prompt: "Use $om-harness only as the shared runtime for staged-only wrapper workflows."
policy:
  allow_implicit_invocation: false
